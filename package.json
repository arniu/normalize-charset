{
  "name": "normalize-charset",
  "version": "0.2.0",
  "description": "Normalize charset",
  "main": "index.js",
  "scripts": {
    "test": "mocha --reporter spec --bail --check-leaks test/",
    "lint": "prettier --write **/*.js; standard --fix",
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "*.js": [
      "npm test",
      "prettier --write",
      "standard --fix",
      "git add"
    ]
  },
  "standard": {
    "env": {
      "mocha": true
    }
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/arniu/normalize-charset.git"
  },
  "keywords": [
    "charset",
    "connect",
    "express"
  ],
  "author": "Arniu Tseng <arniu2006@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/arniu/normalize-charset/issues"
  },
  "homepage": "https://github.com/arniu/normalize-charset#readme",
  "devDependencies": {
    "content-type": "^1.0.4",
    "express": "^4.16.2",
    "husky": "^0.14.3",
    "lint-staged": "^6.0.0",
    "mocha": "^4.0.1",
    "prettier": "^1.8.2",
    "standard": "^10.0.3",
    "supertest": "^3.0.0"
  }
}
